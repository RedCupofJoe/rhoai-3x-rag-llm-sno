global:
  pattern: rag-llm-sno
  # HashiCorp Vault is not used by this pattern (no External Secrets / vault-backend).
  secretLoader:
    disabled: true
main:
  clusterGroupName: prod
  multiSourceConfig:
    enabled: true
    clusterGroupChartVersion: 0.9.*
